# Portfolio 2.0 - FSD Architecture

## 📁 Структура проекта

Проект следует **Feature-Sliced Design (FSD)** архитектуре с использованием Next.js App Router.

```
src/
├── app/                    # Next.js App Router
│   ├── layout.tsx         # Root layout с ProfileSidebar + Navigation
│   ├── page.tsx           # Redirect на /player
│   ├── player/page.tsx    # Страница Player
│   ├── exp/page.tsx       # Страница Experience
│   ├── skills/page.tsx    # Страница Skills
│   ├── contact/page.tsx   # Страница Contact
│   └── globals.css        # Глобальные стили
│
├── widgets/               # Комплексные UI блоки
│   ├── ProfileSidebar/   # Левый sidebar с аватаром (в layout)
│   ├── PlayerInfo/       # Контент страницы Player
│   ├── ExperienceInfo/   # Контент страницы Exp
│   ├── SkillsInfo/       # Контент страницы Skills
│   └── ContactInfo/      # Контент страницы Contact
│
└── shared/               # Переиспользуемые компоненты
    └── ui/              # UI компоненты
        ├── Card/        # Карточка с заголовком
        ├── Navigation/  # Навигация между страницами
        ├── IdCard/      # ID карточка
        └── PixelCorner/ # Пиксельные углы
```

## 🎨 Дизайн система

- **Primary Color**: `#FF00B2` (ярко-розовый)
- **Шрифты**: 
  - Press Start 2P (pixel font)
  - JetBrains Mono (monospace)
- **Стиль**: Retro-gaming aesthetic

## 🏗️ Архитектурные решения

### Роутинг
- Используется **Next.js App Router** для навигации между страницами
- Главная страница (`/`) редиректит на `/player`
- Навигация через `Navigation` компонент в layout

### Layout
- **Левая часть** (35% ширины): `ProfileSidebar` - статичная, не меняется между страницами
- **Правая часть** (остальное): Динамический контент с навигацией вверху

### Компоненты
- **Widgets**: Сложные блоки страницы, могут содержать бизнес-логику
- **Shared/UI**: Переиспользуемые UI компоненты без бизнес-логики

## 🚀 Запуск

```bash
bun run dev
```

Проект доступен на http://localhost:3000
